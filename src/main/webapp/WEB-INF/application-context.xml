<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:context="http://www.springframework.org/schema/context"
    xmlns:mybatis="http://mybatis.org/schema/mybatis-spring"
    xsi:schemaLocation="http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd
        http://mybatis.org/schema/mybatis-spring https://mybatis.org/schema/mybatis-spring.xsd">
<!--
    <bean id="hikariConfig" class="com.zaxxer.hikari.HikariConfig">
         hikariConfig.setDriverClassName("");
        <property name="driverClassName"
            value="oracle.jdbc.OracleDriver" />
        <property name="jdbcUrl"
            value="jdbc:oracle:thin:@192.168.20.11:1521:xe" />
        <property name="username" value="ohdogcat" />
        <property name="password" value="raiseme" />
    </bean>

     HikariDataSrouce ds = new HikariDataSource(config);
    <bean id="ds" class="com.zaxxer.hikari.HikariDataSource">
        <constructor-arg ref="hikariConfig" />
    </bean>

     SqlSessionFactoryBean: 데이터 소스(커넥션 풀)을 이용해서 SQL 문장들을 실행/결과 처리를 하고
        리턴하는 객체.
    <bean id="session"
        class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="ds" />
        <property name="mapperLocations"
            value="classpath:/mappers/**/*.xml" />
        <property name="configLocation"
            value="classpath:mybatis-config.xml" />
    </bean>

    <mybatis:scan
        base-package="com.ohdogcat.repository" />

    <context:component-scan
        base-package="com.ohdogcat.service" />
        -->
</beans>